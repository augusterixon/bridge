package com.bridge.device.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

object BridgeColors {
    val background = Color(0xFF0A0A0A)

    // Tile backgrounds
    val tilePrimary = Color(0xFF1A1A1A)
    val tileWhatsApp = Color(0xFF0D2410)
    val tileUtility = Color(0xFF161616)

    // Borders
    val borderDefault = Color(0xFF2A2A2A)
    val borderWhatsApp = Color(0xFF1A3A1A)
    val borderUtility = Color(0xFF242424)

    // Text
    val textPrimary = Color.White
    val textSecondary = Color(0xFFCCCCCC)
    val textMuted = Color(0xFF888888)
    val textDimmed = Color(0xFF666666)
    val textUltraMuted = Color(0xFF777777)

    // Accent colors
    val whatsApp = Color(0xFF25D366)
    val mapsBlue = Color(0xFF4285F4)
    val spotifyGreen = Color(0xFF1DB954)

    // Wordmark / header
    val wordmark = Color(0xFF666666)
    val clock = Color(0xFF888888)

    // Home indicator
    val homeIndicator = Color(0xFF333333)

    // Medium row
    val rowChevron = Color(0xFF666666)

    // Utility labels
    val utilityLabel = Color(0xFF888888)
    val utilityIcon = Color(0xFF999999)
}