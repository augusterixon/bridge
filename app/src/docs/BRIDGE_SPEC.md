	•	Goal: Bridge is the only usable experience (kiosk / non-escapable).
	•	Model: Fully provisioned device sold ready-to-use, Device Owner via Managed Google Play.
	•	App policy: Users cannot install apps. Only Bridge can enable/disable a curated set (auth, payments, comms, travel).
	•	Non-negotiables: BankID, Microsoft Auth, payments.
	•	UX: same philosophy as website: calm, minimal, intentional.
	•	Implementation primitives: lock task, device owner, boot relaunch, disable status bar/keyguard (where allowed).
	•	Current code status: (brief bullets of what’s already implemented)
